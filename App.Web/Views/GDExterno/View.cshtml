@model App.Model.GestionDocumental.GD

@Html.Action("Header", "Proceso", new { Model.ProcesoId })

<div class="container">

    @Html.Partial("_Message")

    <div class="row">
        <div class="col s12 m6 l5">
            <div class="card">
                <div class="card-content">
                    <span class="card-title">Información del ingreso externo</span>
                    <h3>ID @Model.ProcesoId</h3>
                    <div class="row">
                        <div class="input-field col s12 m10">
                            <strong>@Html.DisplayNameFor(m => Model.Proceso.NombreFuncionario)</strong><br />
                            @Html.DisplayFor(m => Model.Proceso.NombreFuncionario) @Html.DisplayFor(m => Model.Proceso.Email)
                        </div>

                        <div class="input-field col s12 m10">
                            <strong>@Html.DisplayNameFor(m => Model.Fecha)</strong><br />
                            @string.Format("{0:dd-MM-yyyy HH:mm:ss}", Model.Fecha)
                        </div>
                        <div class="input-field col s12 m10">
                            <strong>@Html.DisplayNameFor(m => Model.FechaIngreso)</strong><br />
                            @string.Format("{0:dd-MM-yyyy}", Model.FechaIngreso)
                        </div>
                        <div class="input-field col s12 m10">
                            <strong>@Html.DisplayNameFor(m => Model.GDOrigenId)</strong><br />
                            @Html.DisplayFor(m => Model.GDOrigen.Descripcion)
                        </div>
                        @if (!string.IsNullOrWhiteSpace(Model.Materia))
                        {
                            <div class="input-field col s12 m10">
                                <strong>@Html.DisplayNameFor(m => Model.Materia)</strong><br />
                                @Html.DisplayFor(m => Model.Materia)
                            </div>
                        }
                        <div class="input-field col s12 m10">
                            <strong>@Html.DisplayNameFor(m => Model.NumeroExterno)</strong><br />
                            @Html.DisplayFor(m => Model.NumeroExterno)
                        </div>
                        @if (!string.IsNullOrWhiteSpace(Model.Referencia))
                        {
                            <div class="input-field col s12 m10">
                                <strong>@Html.DisplayNameFor(m => Model.Referencia)</strong><br />
                                @Html.DisplayFor(m => Model.Referencia)
                            </div>
                        }
                        @if (!string.IsNullOrWhiteSpace(Model.Observacion))
                        {
                            <div class="input-field col s12 m10">
                                <strong>@Html.DisplayNameFor(m => Model.Observacion)</strong><br />
                                @Html.DisplayFor(m => Model.Observacion)
                            </div>
                        }
                        <div class="input-field col s12 m10">
                            <strong>@Html.DisplayNameFor(m => Model.DestinoFuncionarioEmail)</strong><br />
                            @Html.DisplayFor(m => Model.DestinoFuncionarioNombre) (@Html.DisplayFor(m => Model.DestinoFuncionarioEmail))<br />
                            @Html.DisplayFor(m => Model.DestinoUnidadDescripcion) (@Html.DisplayFor(m => Model.DestinoUnidadCodigo))
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col s12 m6 l7">
            @Html.Action("Documents", "GDExterno", new { Model.ProcesoId })
        </div>
        <div class="col s12 m12 l12">
            @Html.Action("Workflow", "GDExterno", new { Model.ProcesoId })
        </div>
    </div>
</div>